apiVersion: v1
kind: Service
metadata:
  name: {{ include "vigilops.fullname" . }}-backend
  labels:
    {{- include "vigilops.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vigilops.backend.selectorLabels" . | nindent 4 }}
